<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>OrbViz · Orbital Mechanics Tools</title>
<style>
@import url('https://fonts.googleapis.com/css2?family=Rajdhani:wght@300;400;600;700&family=Share+Tech+Mono&display=swap');

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

:root {
  --bg:        #040d14;
  --bg2:       #071520;
  --cyan:      #00e5ff;
  --green:     #00ffa0;
  --orange:    #ff8840;
  --dim:       rgba(160,210,255,.45);
  --border:    rgba(0,229,255,.12);
  --card-bg:   rgba(0,229,255,.04);
  --card-hover:rgba(0,229,255,.09);
}

html, body {
  height: 100%;
  background: var(--bg);
  color: #c8e0f0;
  font-family: 'Rajdhani', sans-serif;
  font-size: 16px;
  line-height: 1.6;
  overflow-x: hidden;
}

/* ── Star field ── */
canvas#stars {
  position: fixed;
  inset: 0;
  z-index: 0;
  pointer-events: none;
}

/* ── Layout ── */
.page {
  position: relative;
  z-index: 1;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 0 24px 80px;
}

/* ── Header ── */
header {
  width: 100%;
  max-width: 860px;
  padding: 56px 0 48px;
  text-align: center;
}

.badge {
  display: inline-block;
  font-family: 'Share Tech Mono', monospace;
  font-size: 10px;
  letter-spacing: .18em;
  color: var(--cyan);
  border: 1px solid rgba(0,229,255,.30);
  padding: 3px 10px;
  border-radius: 2px;
  margin-bottom: 20px;
  text-transform: uppercase;
}

h1 {
  font-size: clamp(2.8rem, 7vw, 5rem);
  font-weight: 700;
  letter-spacing: .04em;
  line-height: 1.0;
  color: #e8f4ff;
  text-shadow: 0 0 60px rgba(0,229,255,.18);
}

h1 span {
  color: var(--cyan);
}

.tagline {
  margin-top: 16px;
  font-size: 1.1rem;
  font-weight: 300;
  color: var(--dim);
  letter-spacing: .04em;
  max-width: 480px;
  margin-left: auto;
  margin-right: auto;
}

/* ── Divider ── */
.rule {
  width: 100%;
  max-width: 860px;
  height: 1px;
  background: linear-gradient(90deg, transparent, var(--border) 20%, var(--border) 80%, transparent);
  margin: 0 auto 56px;
}

/* ── Cards ── */
.cards {
  width: 100%;
  max-width: 860px;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(340px, 1fr));
  gap: 20px;
}

.card {
  background: var(--card-bg);
  border: 1px solid var(--border);
  border-radius: 4px;
  padding: 32px 28px 28px;
  text-decoration: none;
  color: inherit;
  display: flex;
  flex-direction: column;
  gap: 16px;
  transition: background .2s, border-color .2s, transform .2s;
  cursor: pointer;
}

.card:hover {
  background: var(--card-hover);
  border-color: rgba(0,229,255,.28);
  transform: translateY(-2px);
}

.card-eyebrow {
  font-family: 'Share Tech Mono', monospace;
  font-size: 9px;
  letter-spacing: .20em;
  text-transform: uppercase;
  color: var(--dim);
}

.card-title {
  font-size: 1.65rem;
  font-weight: 700;
  letter-spacing: .03em;
  color: #e8f4ff;
  line-height: 1.1;
}

.card-title span { color: var(--cyan); }
.card.green .card-title span { color: var(--green); }
.card.green { border-color: rgba(0,255,160,.12); background: rgba(0,255,160,.03); }
.card.green:hover { border-color: rgba(0,255,160,.28); background: rgba(0,255,160,.07); }
.card.green .card-eyebrow { color: rgba(0,255,160,.50); }
.card.green .card-btn { background: rgba(0,255,160,.12); color: var(--green); border-color: rgba(0,255,160,.30); }
.card.green .card-btn:hover { background: rgba(0,255,160,.22); }

.card-desc {
  font-size: .95rem;
  font-weight: 400;
  color: rgba(160,210,255,.70);
  line-height: 1.65;
  flex: 1;
}

.card-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
}

.tag {
  font-family: 'Share Tech Mono', monospace;
  font-size: 9px;
  letter-spacing: .12em;
  text-transform: uppercase;
  padding: 3px 8px;
  border-radius: 2px;
  background: rgba(0,229,255,.07);
  color: rgba(0,229,255,.60);
  border: 1px solid rgba(0,229,255,.14);
}

.card.green .tag {
  background: rgba(0,255,160,.07);
  color: rgba(0,255,160,.60);
  border-color: rgba(0,255,160,.14);
}

.card-btn {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  margin-top: 4px;
  padding: 9px 18px;
  font-family: 'Rajdhani', sans-serif;
  font-size: .85rem;
  font-weight: 600;
  letter-spacing: .10em;
  text-transform: uppercase;
  color: var(--cyan);
  background: rgba(0,229,255,.10);
  border: 1px solid rgba(0,229,255,.28);
  border-radius: 3px;
  transition: background .15s;
  align-self: flex-start;
}

.card-btn:hover { background: rgba(0,229,255,.20); }

.card-btn svg {
  width: 13px; height: 13px;
  stroke: currentColor; fill: none;
  stroke-width: 2; stroke-linecap: round; stroke-linejoin: round;
  flex-shrink: 0;
}

/* ── Footer ── */
footer {
  margin-top: 72px;
  font-family: 'Share Tech Mono', monospace;
  font-size: 10px;
  letter-spacing: .12em;
  color: rgba(100,160,200,.30);
  text-align: center;
}

/* ── Responsive ── */
@media (max-width: 480px) {
  header { padding: 40px 0 36px; }
  .cards { grid-template-columns: 1fr; }
  .card { padding: 24px 20px 22px; }
}
</style>
</head>
<body>

<canvas id="stars"></canvas>

<div class="page">
  <header>
    <div class="badge">Aerospace Visualization Suite</div>
    <h1>Orb<span>Viz</span></h1>
    <p class="tagline">Interactive tools for orbital mechanics and cislunar mission analysis</p>
  </header>

  <div class="rule"></div>

  <div class="cards">

    <!-- Orbital Parameters -->
    <a class="card" href="orbviz.html">
      <div class="card-eyebrow">Tool 01 · Earth Orbit</div>
      <div class="card-title">Orbital <span>Parameters</span></div>
      <p class="card-desc">
        Explore the six classical Keplerian elements in real time. Visualize how RAAN, inclination, eccentricity, and argument of perigee shape an orbit around Earth — with Lambert shading, J2 precession, ECI/ECF frame switching, and a physics-correct animated ground track.
      </p>
      <div class="card-tags">
        <span class="tag">Keplerian Elements</span>
        <span class="tag">ECI / ECF</span>
        <span class="tag">J2 Precession</span>
        <span class="tag">Ground Track</span>
        <span class="tag">Lambert Shading</span>
      </div>
      <div class="card-btn">
        <svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="3"/><path d="M12 2a10 10 0 0 1 0 20A10 10 0 0 1 12 2"/><path d="M2 12h20"/><path d="M12 2c2.5 4 4 6.5 4 10s-1.5 6-4 10"/><path d="M12 2c-2.5 4-4 6.5-4 10s1.5 6 4 10"/></svg>
        Launch Tool
      </div>
    </a>

    <!-- Cislunar Explorer -->
    <a class="card green" href="cislunar_explorer.html">
      <div class="card-eyebrow">Tool 02 · Cislunar Space</div>
      <div class="card-title">Cislunar <span>Explorer</span></div>
      <p class="card-desc">
        Navigate the Earth–Moon system. Visualize Lagrange points, halo orbit families, and low-energy transfer trajectories across cislunar space — the contested frontier between GEO and lunar orbit that's reshaping space domain awareness.
      </p>
      <div class="card-tags">
        <span class="tag">Lagrange Points</span>
        <span class="tag">Halo Orbits</span>
        <span class="tag">Earth–Moon System</span>
        <span class="tag">Transfer Trajectories</span>
      </div>
      <div class="card-btn">
        <svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="3"/><path d="M12 2a10 10 0 0 1 0 20A10 10 0 0 1 12 2"/><path d="M2 12h20"/><path d="M12 2c2.5 4 4 6.5 4 10s-1.5 6-4 10"/><path d="M12 2c-2.5 4-4 6.5-4 10s1.5 6 4 10"/></svg>
        Launch Tool
      </div>
    </a>

  </div>

  <footer>ORBVIZ · AEROSPACE VISUALIZATION · BUILT WITH VANILLA JS + CANVAS 2D</footer>
</div>

<script>
// ── Starfield ──────────────────────────────────────────────────────────
const canvas = document.getElementById('stars');
const ctx = canvas.getContext('2d');
let stars = [];

function initStars(){
  canvas.width  = window.innerWidth;
  canvas.height = window.innerHeight;
  stars = Array.from({length: 280}, () => ({
    x: Math.random() * canvas.width,
    y: Math.random() * canvas.height,
    r: Math.random() * 1.1 + 0.2,
    a: Math.random() * 0.7 + 0.15,
    speed: Math.random() * 0.015 + 0.005,
    phase: Math.random() * Math.PI * 2
  }));
}

function drawStars(t){
  ctx.clearRect(0, 0, canvas.width, canvas.height);
  for(const s of stars){
    const alpha = s.a * (0.6 + 0.4 * Math.sin(t * s.speed + s.phase));
    ctx.beginPath();
    ctx.arc(s.x, s.y, s.r, 0, Math.PI*2);
    ctx.fillStyle = `rgba(180,220,255,${alpha.toFixed(2)})`;
    ctx.fill();
  }
  requestAnimationFrame(drawStars);
}

window.addEventListener('resize', initStars);
initStars();
requestAnimationFrame(drawStars);
</script>
</body>
</html>
